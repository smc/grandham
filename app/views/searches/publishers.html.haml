.row
  = render partial: 'search_nav_links', locals: { activated_page: :publishers }

%br
.container
  .row
    .col-md-8.col-offset-1
      %form{ action: publishers_search_path, method: 'get' }
        .form-group
          %input{ name: 'query', value: params[:query], class: 'input-xxlarge search-field', placeholder: 'Search publishers' }
          %button.btn-success{ type: 'submit', class: 'btn btn-primary' }
            %span.glyphicon-search.glyphicon
            = I18n.t('.search')
      %br
      - if @results && @results.any?
        = render @results
        = will_paginate @results, renderer: BootstrapPagination::Rails
