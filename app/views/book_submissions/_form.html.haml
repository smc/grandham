= form_for @book_submission do |f|
  - if @book_submission.errors.any?
    #error_explanation
      %h2= "#{pluralize(@book_submission.errors.count, "error")} prohibited this book_submission from being saved:"
      %ul
        - @book_submission.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :book_id
    = f.number_field :book_id
  .actions
    = f.submit 'Save'
