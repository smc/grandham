.container
  .row
    .span7.offset1
      %h3= inplace_edit @author, :name

      %h4 Books

      %ul.associated-books-list
        - @author.books.each do |book|
          %li
            %i.icon-book
            = link_to language_book_path(book.language, book) do
              = book.title